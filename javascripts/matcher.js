define([],
	function(){

// grab object of users
// console.log("howdy", importUserArray);
// subtract this user

// filter remaining objects in array by gender, age

// for remaining 'eligible' objects:

// 	subscore1 = (n-average)/(max-min);

// 	take input from common interests

// 	+1 for each in common

// 	subscore2 = (n-average)/(max-min);
	




























	});